apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: dev
images:
  - name: unedotamps/api-app
    newTag: latest

replicas:
  - name: api-app
    count: 3
